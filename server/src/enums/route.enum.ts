export enum USER_PATHS {
  GET_USERS = "/get-users/:id",
}

export enum AUTH_PATHS {
  LOGIN = "/login",
  REGISTER = "/register",
}

export enum MESSAGE_PATHS {
  SEND = "/send",
  GET_ALL = "/get-all",
}
